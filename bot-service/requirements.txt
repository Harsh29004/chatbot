# The bot pipeline's real dependencies. This is why it stayed in Python.

# Embeddings. The model that produced every vector already in the database.
sentence-transformers>=3.0.0

# Vector similarity for the MongoDB-backed store.
numpy>=1.26

# MongoDB. The sync driver, not motor: every store call already runs in a
# worker thread, and mixing an async driver in would mean two concurrency
# models in one request path.
pymongo>=4.6

# The retrieval graph.
langgraph>=0.2.0
langchain-core>=0.3.0

# Sheet reading. pandas is the spine; the rest are the optional readers it
# dispatches to, and bot/readers.py says so plainly when one is missing.
pandas>=2.2.0
openpyxl>=3.1.0
#   pypdf   -> .pdf        pyarrow -> .parquet
#   xlrd    -> legacy .xls odfpy   -> .ods
#   PyYAML  -> .yaml       beautifulsoup4 -> .html
pypdf>=4.0
PyYAML>=6.0
beautifulsoup4>=4.12
lxml>=5.0

# The service itself.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
# FastAPI needs this to accept an uploaded file at all — without it the upload
# endpoint raises at request time rather than at import time.
python-multipart>=0.0.9

# httpx is what llm.py talks to Ollama, Groq and Gemini with.
httpx>=0.27.0
